// Подключаем свою функцию
var sum = require('./index.js');

// Передаем в функцию два числа и выводим результат - 45
console.info(sum(12, 33));  
// Передаем в функцию два числа и выводим результат - 118
console.info(sum(101, '17'));  